import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Belcreation — регистрация на кемп",
};

export default function BookingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
